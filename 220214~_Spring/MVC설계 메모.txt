(뷰 만들기)
컨트롤러 만들기(화면처리, 값 받아서 처리+매핑) 
	- 값 받아서 처리하는건 service에서! 당장 받은 값 임시 저장은 VO에서!
=> Service 인터페이스&클래스 + VO  만들기 (당장 받은 값을 처리하는 매서드 - DAO에서 사용) 
	- DB와 연결 및 작업은 DAO에서!
=> DAO만들기

? 서비스 기준으로 받을값/넘길값을 생각하기
? 컨트롤러는 서비스나 뷰로 매핑해주는 역할
? VO는 지금 받은 값을 저장해둠
? 서비스가 DB와의 작업이 필요하다면, DB와 직접 연동하여 작업을 처리하는 DAO로 연결해주는 역할
? 실제로 처리에 관련된 코드는 DAOImpl에서 작성하게 된다

부모>자식
root context > servlet-context