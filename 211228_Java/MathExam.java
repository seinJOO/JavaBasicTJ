package Java20211228;
import java.lang.Math.*; //Math매서드 import

public class MathExam {

	public static void main(String[] args) {
/*		
		- Math는 기본적인 수학 계산에 유용한 매서드들로 구성되어 있음 (random() round() ...)
		- Math의 생성자는 접근제어자가 private()이기 때문에 다른 클래스에서는 Math객체를 생성할 수 없음
		- Math의 매서드는 모두 static으로 사용한다 !! (매서드 사용 시 앞에 Math. 붙이기)
		
		1) ceil() : 올림 / round(): 반올림 / floor() : 버림
		
				double val = 90.7552;
		
				System.out.println(val);
				System.out.println(Math.ceil(val)); 	//올림
				System.out.println(Math.round(val));	//반올림		
				System.out.println(Math.floor(val));	//버림
				
				System.out.println(Math.ceil(val*100)/100); 			//소수점 둘째자리까지 올림
				System.out.println((double)Math.round(val*100)/100);	//소수점 둘째자리까지 반올림	?????		
				System.out.println(Math.floor(val*100)/100);			//소수점 둘째자리까지 버림
				String val2 = String.format("%.2f", val);
				System.out.println(val2);
		
		2) random() 
		
				예시 1) double d의 난수 발생	-	0.0 <= d <1.0 -> %값 형태로 난수 발생
					double d = Math.random();		
					int i = (int) (Math.random() * 10); // 난수를 일의 단위로 발생시킨 후 정수로 형변환
*/	
		
		
		
		
		
		
	}

}
