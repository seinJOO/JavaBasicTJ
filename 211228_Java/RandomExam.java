package Java20211228;
import java.util.Random;
public class RandomExam {

	public static void main(String[] args) {
/*		
	Random() 클래스
	- 시드값을 설정할 수 있음 (난수를 구하는 Math.random()과 가장 큰 차이점)
	
	- 시드값이 같은 Random 객체들은 항상 같은 난수를 가짐	
	-> 예시)  같은 공식에 같은 값을 넣으면 같은 결과를 얻는 것과 같이, 같은 난수를 얻게 됨	
		Random rand = new Random(10);
		Random rand2 = new Random(10);
		
		for (int i = 0 ; i < 3 ; i++) {
			System.out.println(rand.nextInt());
		}
		for (int i = 0 ; i < 3 ; i++) {
			System.out.println(rand2.nextInt());
		}
	
	
		
		
*/
		
		
		
	}

}
